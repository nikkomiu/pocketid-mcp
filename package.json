{
  "name": "pocketid-mcp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/pocketid-mcp",
    "type-check": "tsc --noEmit",
    "lint": "biome lint ./src",
    "format": "biome format ./src"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "pino": "^9.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "biome": "latest",
    "typescript": "^5.0.0"
  }
}
